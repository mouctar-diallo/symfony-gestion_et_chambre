{% extends 'layouts/base.html.twig'%}

{% block body %}
    <div class="card-header">
        <h2 class="text-center mt-4">Modifier la Chambre</h2>
    </div>
    <div class="align-items-center justify-content-center d-flex mt-4">
        {{ form_start(form_edit) }}
            {{ form_widget(form_edit) }}
            <input type="submit" value="modifier" class="btn btn-primary btn-block" name="enregistrer">
        {{ form_end(form_edit) }}
    </div>
{% endblock %}